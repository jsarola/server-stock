# Connexió a la base de dades PostgreSQL
DATABASE_URL=postgresql://usuari:contrasenya@localhost:5432/server_stock

# Port del servidor web (opcional, per defecte 3000)
PORT=3000

# Carrega dades de demo des de server_stock/demo_data.xml en arrencar (opcional)
# LOAD_DEMO_DATA=true
